

const initialState: number = 0

export const minReducer = (state = initialState, action: any) => {
    switch (action.type) {
        case "":
            return state
        default:
            return state
    }
}