

const initialState:number = 1

export const maxReducer = (state = initialState, action: any) => {
    switch (action.type) {
        case "":
            return state
        default:
            return state
    }
}